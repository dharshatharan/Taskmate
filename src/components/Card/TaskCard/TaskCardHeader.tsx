import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

interface Props {
    
}

const TaskCardHeader = (props: Props) => {
    return (
        <View>
            <Text></Text>
        </View>
    )
}

export default TaskCardHeader

const styles = StyleSheet.create({})
